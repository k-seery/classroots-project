3.3. Create the .env File
In the backend folder, create a file named .env.

Paste the following content (replace values as needed):

env
Copy
PORT=3000
DB_URI=mongodb+srv://kseery:9zG4oz3q9ffxC7PW@classroots-cluster.6evly.mongodb.net/
JWT_SECRET=your_production_jwt_secret_here
OPENAI_API_KEY=your_openai_api_key
CORS_ORIGIN=*